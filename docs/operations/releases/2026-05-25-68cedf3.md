---
status: historical
owner: operations
last_reviewed: 2026-05-25
superseded_by:
---

# Production Release 2026-05-25 68cedf3

## Summary

Deployed the session handle, theme submission, and boost loop to the Hetzner production VPS after merging PR #9.

## Environment

- Provider: Hetzner Cloud
- Project: `strangedreamz`
- Environment: production
- Server: `strangedreamz-prod-ash-1`
- Public URL: `http://87.99.136.176`
- Runtime: Docker Compose

## Source

- Commit: `68cedf304d9101c1462fe8aaca2ab40c05c5fc3a`
- Short SHA: `68cedf3`
- Branch: `main`
- PR: `https://github.com/Xibalba1/strangedreamz/pull/9`
- CI: GitHub Actions `Validation / validate` passed before merge

## Deployment

Deployment followed `docs/operations/deployment-runbook.md`:

```bash
cd /opt/strangedreamz
git fetch origin
git checkout main
git reset --hard origin/main
RELEASE_SHA="$(git rev-parse HEAD)"
printf "RELEASE_SHA=%s\n" "$RELEASE_SHA" > deploy/.env
docker compose -f deploy/compose.yaml up -d --build
```

Post-deploy service status showed:

- `deploy-app-1`: up past the 10-minute validation window
- `deploy-caddy-1`: up

## Release Identity

`/healthz` reported:

```json
{
  "ok": true,
  "service": "strangedreamz",
  "status": "Deterministic four-panel shell is ready for the first shared room proof.",
  "release": {
    "sha": "68cedf304d9101c1462fe8aaca2ab40c05c5fc3a"
  }
}
```

## Smoke Checks

Immediate smoke:

```bash
npm run smoke -- --url http://87.99.136.176 --expected-sha 68cedf304d9101c1462fe8aaca2ab40c05c5fc3a
```

Result: passed.

Follow-up smoke after the post-deploy validation window:

```bash
npm run smoke -- --url http://87.99.136.176 --expected-sha 68cedf304d9101c1462fe8aaca2ab40c05c5fc3a
```

Result: passed.

Changed-surface checks:

```bash
curl -sS -i http://87.99.136.176/display
curl -sS -i 'http://87.99.136.176/api/room?sessionId=smoke-session-a-68cedf3'
```

Result: `/display` and `/api/room` returned `HTTP/1.1 200 OK`.

The production JSON action loop also accepted:

- handle claim for `Smoke A 68cedf3`
- theme submission for `Production smoke theme 68cedf3`
- handle claim for `Smoke B 68cedf3`
- cross-session boost of the submitted theme, returning `1` boost

## Log Check

Recent Docker Compose logs showed normal app startup and successful `GET /healthz`, `GET /`, `GET /display`, `GET /api/room`, `POST /api/handles`, `POST /api/themes`, and `POST /api/themes/:themeId/boosts` requests.

No app crash loop or failed Strangedreamz route was observed. Caddy logs contained unrelated public internet `CONNECT www.google.com:443` proxy-scan attempts from an external IP; those returned `502` and were not app traffic.

## Rollback

Rollback path is documented in `docs/operations/rollback-runbook.md`.

Previous production SHA:

```text
a2d7cdd21d2d64c40d6374882c2e7e5933df7e67
```
